# ComfyUI AI Assistant 依赖项

# 基础包
aiohttp>=3.8.0  # 用于异步HTTP服务和客户端
pillow>=9.0.0   # 图像处理

# AI服务接口
g4f>=0.1.9      # 用于免费GPT服务
requests>=2.28.0  # HTTP客户端库（某些G4F提供商可能使用）

# 工具包
# python-dotenv>=0.20.0  # 环境变量管理（可选，用于API密钥管理）
# tqdm>=4.64.0           # 进度条（可选，用于大型文件上传）

# 数据处理
# ujson>=5.5.0           # 更快的JSON处理（可选） 